# Legacy Python Archive

This directory contains the previous Flask/Python implementation that used:

- `app.py` and `run.py` for the web app
- `templates/` for the Flask UI
- `transcript_toolkit/` and `youtube_channel_analyzer.py` for transcript and channel helpers
- Python-focused setup and documentation

These files are archived for reference only. The supported application now lives in the Cloudflare Worker app at the repository root.
